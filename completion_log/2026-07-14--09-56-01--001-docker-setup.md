# 001 Docker Setup Re-verification

## Summary

Random completed-spec verification for 001-docker-setup found and fixed a missing favicon route that caused Chrome to request `/favicon.ico` and receive a 404.

## Changes

- Added `favicon.svg`.
- Served the icon from `/ralphi/favicon.svg` and `/favicon.ico`.
- Included the icon in the Docker image.
- Added the icon link to `index.html`.
- Incremented `NR_OF_TRIES` to 2.

## Verification

- `docker compose config` passed.
- `docker compose up -d --build` rebuilt and recreated `ralphi`.
- Docker health reported `running healthy`.
- `http://localhost:8081/healthz` returned 200.
- `http://localhost:8081/ralphi/`, `/ralphi/styles.css`, `/ralphi/app.js`, and `/ralphi/favicon.svg` returned 200.
- `http://localhost:18080/ralphi/` and `/ralphi/favicon.svg` returned 200 via Caddy.
- `https://mac-mini-von-est.tailfb7eca.ts.net/ralphi/` and `/ralphi/favicon.svg` returned 200.
- Headless Chrome desktop and mobile rendered the status page with "Ralphi – Ralph Wiggum Loop Manager", "Status: Alive", current date text, and the Ralph loop hint.
- Headless Chrome reported no failed requests, no 4xx/5xx responses, no JavaScript exceptions, no console errors, and no horizontal overflow.
